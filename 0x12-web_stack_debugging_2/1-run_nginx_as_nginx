#!/usr/bin/env bash
# run nginx as nginx user listening on port 8080
pkill -f apache2
sed -i "s/#user www-data/user nginx/" /etc/nginx/nginx.conf
sed -i "s/80/8080/g" /etc/nginx/sites-available/default
sudo -u nginx service nginx restart

